# MVP 边界

MVP 的定义是"从打开文件夹到看到 PDF 的全链路跑通"：项目 + 文件树 + 标签页 + 编辑（高亮/查找替换）+ 双模式编译 + 调度全套 + 错误列表跳转 + 内嵌预览（含 SyncTeX）。LSP、设置页完整化、折叠/多光标等叠加价值后置。

- **状态**：已接受
- **备选方案**：LSP 进 MVP（否决：是叠加价值不是链路必需，且引入外部进程依赖）
- **影响**：增量编译在 MVP 完成后作为**首要任务**（它是延迟预算优秀线的必经之路）
